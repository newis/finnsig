from setuptools import setup, find_packages

setup(
    name="finnsig",
    version="0.1.0",
    description="Generate Finn-Gw-Key used in HTTP headers for the FINN.no app.",
    packages=find_packages(),
)
